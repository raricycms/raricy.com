# 行尾统一 LF。
#
# 为什么显式声明：SCSS 源与 sass 的产物输出的都是纯 LF，而 Windows 上
# core.autocrlf=true 的仓库期待工作区是 CRLF。两边不一致时 git 会挂一个
# **内容其实完全相同**的假 " M"（当时核对过 blob 与工作区的哈希一致），
# 并且在 `git pull` 时被它挡下：
#   error: Your local changes to the following files would be overwritten by merge
# 实测踩过。声明 eol=lf 后两边预期一致，这个假 M 不再出现。
#
# 历史上这条规则主要保的是入库的编译产物；它已退出版本库
# （见 .gitignore），但规则对 SCSS 源与 tests/.tmp 下的手工探针产物同样有意义，
# 故保留。
*.css text eol=lf
*.scss text eol=lf

# *.sql 是后补的，理由与样式那两条不同：
#
# `scripts/migrate.mjs` 的 verify 对迁移文件**全文**取 sha256（注释、行尾都算），
# 而入库的迁移在 autocrlf=true 的机器上检出成 CRLF、在别的机器上是 LF ——
# 同一份提交算出两个哈希。后果不是报错而是**恒假的漂移告警**：本机实测 3 个
# 迁移（3_fish_integer_units / 4_chat / 11_comment_attachments）报 checksum
# 不一致，与 SQL 内容毫无关系，白淹没真问题。
#
# 钉成 LF 后各平台一致。注意它**不是**为了让文件「变干净」：
# verify 比的是工作区文件，所以改完要重新检出一次；而**已经应用过**迁移的库，
# 跟踪表里早先按 CRLF 记的那几个 checksum 会对不上 —— 确认 `git diff` 干净
# （SQL 内容确实没变）后，对报出的每个 `mark` 一次即可；`up` 全程不受影响
# （已应用的一律跳过）。
*.sql text eol=lf
