# 行尾统一 LF。
#
# 为什么必须显式声明：`src/styles-scss/compiled/flask.css` 是 sass 生成的产物，
# 输出的是纯 LF；而 Windows 上 core.autocrlf=true 的仓库期待工作区是 CRLF。
# 两边不一致时 git 会在每次 `npm run build:css` 之后挂一个**内容其实完全相同**的
# 假 " M"（核对过 blob 与工作区哈希一致），并且在 `git pull` 时被它挡下：
#   error: Your local changes to the following files would be overwritten by merge
# 实测踩过。声明 eol=lf 后两边预期一致，这个假 M 不再出现。
#
# 入库的 .css / .scss blob 已全部是 LF，加这行不会让任何现存文件变成「已修改」。
*.css text eol=lf
*.scss text eol=lf
