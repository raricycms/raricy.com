/node_modules
/.next
/out
next-env.d.ts
.env
*.tsbuildinfo
# 本地 SQLite 副本（含真实用户数据，绝不入库）
/prisma/*.db
/prisma/*.db-journal
/prisma/*.db-wal
/prisma/*.db-shm

# 测试临时数据库
tests/.tmp/
coverage/

# Playwright E2E 产物
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# 运行时数据目录（头像 / 图床 / 故事 / 数据库）—— 绝不入库
/instance/

# Python 副产物（历史迁移文档里的代码示例等可能产生）
__pycache__/
*.pyc
