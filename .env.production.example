# ─────────────────────────────────────────────────────────────────────────────
# 生产环境变量模板。复制为 .env.production 并填入真实值。
# ⚠️ 本文件不含任何真实密钥；.env* 已被 .gitignore 排除。
# ─────────────────────────────────────────────────────────────────────────────

# 数据库：指向已【规整为 INTEGER 毫秒】的生产 SQLite（见 docs/instance-restore.md）。
# 从历史库接手时：备份 → 规整 → 确认没有其他进程还在写这个库，再把 DATABASE_URL 指过来。
# ⚠️ 就地规整要写成：npm run db:normalize -- --in-place /绝对路径/instance/database/db.db
#    （`--` 分隔符必须有，否则 npm 会把 --in-place 当成自己的参数吞掉；且 --in-place 后
#     必须跟一个路径。少了任一项都会【静默走复制分支】—— 把 db.db 复制成 dev.db，
#     真正要规整的那个库一个字节都没动。）
# connection_limit=1 + socket_timeout=30：SQLite 是单写库，序列化访问让并发写【排队】
# 而不是抛 Socket timeout(500)。已实测：60 并发读写 → 60×200、0 错误。
# 真正的高并发写扩展仍建议迁 Postgres（届时去掉这两个参数）。
DATABASE_URL="file:/绝对路径/instance/database/db.db?connection_limit=1&socket_timeout=30"

# ⚠️⚠️ 会话与加密密钥 —— 必须与【历史生产环境】完全一致 ⚠️⚠️
#   库里已有的密文是用那套密钥加密的：换成别的值就再也解不开。
#   1) SECRET_KEY：会话签名。换值 → 已下发的会话全部失效（所有人被登出），且下方加密回退失效。
#   2) FISH_ENCRYPTION_KEY：解密 users.fish_api_key_encrypted 的密钥。
#      —— 实测：若此值不对，465 个用户的账户 Key 全部解不开，小鱼干经济全线 503。
#      取值逻辑：优先用 FISH_ENCRYPTION_KEY，未配置才回退 SECRET_KEY。
#      **必须填历史生产 .env 里实际用于加密这批数据的那个密钥**，否则经济系统瘫痪。
SECRET_KEY="<沿用历史生产环境的 SECRET_KEY>"
FISH_ENCRYPTION_KEY="<沿用历史生产环境的 FISH_ENCRYPTION_KEY；若历史数据用 SECRET_KEY 加密则留空>"

# 真实资源目录（部署机器上的数据目录）
IMAGE_UPLOAD_FOLDER="/绝对路径/instance/images"
AVATARS_DIR="/绝对路径/instance/avatars"
STORIES_DIR="/绝对路径/instance/stories"

# 小鱼干账户微服务（FastAPI）—— 必须可达，否则投喂/签到发鱼会 fail-closed(503)
ACCOUNT_SERVICE_URL="http://127.0.0.1:8000"
ACCOUNT_SYSTEM_KEY="<系统账户 API Key>"
ACCOUNT_SERVICE_INTERNAL_TOKEN="<服务间共享令牌>"
ACCOUNT_SERVICE_TIMEOUT="5"

# 鱼干练手盘的行情源（/fish/trade）。
#
# 【默认值是什么】data-api.binance.vision —— 币安拆出来的**纯行情公共域**，不吃 key、
# 不吃签名。之所以不用 api.binance.com：生产服务器是阿里云大陆，实测那个域名被 DNS
# 投毒（解析到 Facebook 的段），CoinGecko / OKX / Coinbase 等一概超时或投毒。
# .vision 这个域实测 200 / ~450ms，解析到真实 AWS 东京 IP、TLS 证书是币安真证书。
#
# ⚠️ 上线前在**这台服务器上**先验一次（整个功能的地基）：
#     curl -s -m 8 -o /dev/null -w "%{http_code}\n" \
#       "https://data-api.binance.vision/api/v3/time"
#   返回 200 才继续。阿里云的出口策略与家宽不同，开发机上通不代表这台通。
#
# 不通时改这一个变量换源即可（api.gateio.ws 实测也可达），不用改代码、不用发版。
# 留空 = 用默认的币安 .vision。
MARKET_PRICE_BASE_URL=""

# 练手盘行情的刷新间隔（毫秒）。**它只刷展示缓存，不服务成交** —— 成交价在下单那一刻
# 现取（见 src/lib/market-price.ts）。所以调大它不影响任何人的成交价，只影响页面上
# 价格的刷新频率。留空 = 15000（15 秒）；置 0 = 关闭这条循环。
MARKET_POLL_MS=""

# CSRF 可信来源（逗号分隔，可写 "example.com" 或 "https://example.com"）。
# 【反向代理必读】中间件判定对外 Host 的顺序：ALLOWED_ORIGINS → X-Forwarded-Host → Host。
#   套 nginx 时，若未配 `proxy_set_header Host $host`，Next 收到的 Host 是上游地址
#   （如 127.0.0.1:3000），与浏览器 Origin 不符 → 正常写请求被误判为 CSRF 403。
#   解决二选一：① nginx 透传 X-Forwarded-Host / Host（推荐，见 docs/deploy.md §6 nginx 反代）
#              ② 在此显式填写站点域名（最稳妥，反代配置不确定时用这个）
ALLOWED_ORIGINS="raricy.com"

# Cloudflare Turnstile（注册页人机验证）
TURNSTILE_AVAILABLE="True"
TURNSTILE_SECRET_KEY="<Turnstile secret>"
NEXT_PUBLIC_TURNSTILE_AVAILABLE="True"
NEXT_PUBLIC_TURNSTILE_SITE_KEY="<Turnstile site key>"

NODE_ENV="production"

# 会话 cookie 的 Secure 标记：留空=自动判定（优先看 nginx 透传的 X-Forwarded-Proto）
#   站点走纯 HTTP 时若下发 Secure cookie，浏览器会丢弃它 →「登录成功但会话不粘」。
#   nginx 若已配 proxy_set_header X-Forwarded-Proto $scheme，此处留空即可自动正确。
#   ⚠️ HTTP 下会话 cookie 明文传输、可被链路窃取冒用 —— 生产站点请上 TLS。
COOKIE_SECURE=""
