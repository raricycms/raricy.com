# Flask → Next.js 架构迁移 · 总览与决策

> 状态：**基础层已落地并跑通**（Prisma 数据层 + 认证/会话 + 博客垂直切片，均已对真实数据库实测通过）。
> 其余业务模块为“按同一套模式复刻”的存量工作，见 [06-剩余工作与里程碑](06-剩余工作与里程碑.md)。

## 0. 先说结论（务必先读）

把这个站点（15 个蓝图、20 张表、Flask-Login 会话、独立的 FastAPI 账户微服务、
Fernet 加密、磁盘图床、CLI 工具、多处限频）**完整**迁到 Next.js，是**数周级**的工程，
不可能在一次无人值守的运行里“全部精修完成”。强行一次性大爆炸重写一个线上站点，
只会得到一个半迁移的坏摊子。

因此本次交付的是**最有价值、且已验证正确**的地基与范式，让后续复刻变成“照着做”：

> 更新（第二轮）：在地基之上，**已把大部分业务模块并行建出并跑通** —— 整站 `next build`
> 全绿（40+ 路由），并对真实数据完成了带鉴权的读写冒烟（登录/注册/评论/投票/签到发鱼/
> 剪贴板/通知/审计…全部 curl 验证通过）。

| 已完成（可运行、已实测） | 说明 |
|---|---|
| ✅ 完整 Prisma schema（20 表 1:1 映射真实库） | `web-next/prisma/schema.prisma`，`prisma validate` 通过 |
| ✅ 数据层实测 | 对真实 `db.db` 完成 typed 读取、多表 join、分类树、角色过滤 |
| ✅ 密码哈希**双向互通** | Node 校验 werkzeug scrypt 哈希；Node 生成的哈希 Flask 也能校验；**注册→登录→Flask 复验**整条链路实测 |
| ✅ 会话/认证层 | JWT 会话 + `session_version` 失效机制（对齐 Flask-Login）|
| ✅ 博客 | 列表/详情/点赞 + **Markdown 客户端渲染**（marked+DOMPurify+highlight.js）+ **楼中楼评论**（增删/点赞）|
| ✅ 认证/用户 | 登录/登出/注册（邀请码升 core）/公开资料/资料编辑/**SVG identicon 头像** |
| ✅ 通知 | 列表/未读数/单条已读/全部已读 + 偏好判定 |
| ✅ 投票 | 列表/详情/创建/投票（每人一票唯一约束）|
| ✅ 签到 + 小鱼干 | 签到抽运势 + 本地发鱼（fail-closed 注释）+ 🥇🥈🥉 排行榜 + 余额/流水 |
| ✅ 剪贴板 / 图床(列表) / 照片墙 / 审计 / 工具(Base·Hex) / 故事列表 | 均建出并冒烟 |
| ✅ 站点地图 / robots / sitemap | Next 约定式 `sitemap.ts` / `robots.ts` |
| ✅ 时间戳规整脚本 + JSON 列读取修复 | 解决 Prisma 读 Python 时间格式 / `JSON` 声明列的两大根因 |
| ✅ 详细迁移文档 | 本目录 7 篇 |

> 更新（第三轮）：又并行补齐一大批——**图床上传**（sharp 压缩+配额+SVG 防护+原生分发）、
> **完整管理后台**（概览/栏目/文章/用户/禁言/申诉裁决/群发）、**账户微服务 TS 客户端**
> （真实 endpoint + Fernet 解密 + 幂等 + fail-closed 投喂）、**投喂 UI**、**故事阅读页**
> （服务端 Markdown）、**Turnstile**、**游戏菜单 + 超级井字棋整局移植**。整站 `next build`
> 全绿，管理端/上传/投喂/群发均已带鉴权 curl 实测通过。

**新增已完成：**
- ✅ 图床上传/删除/分发（sharp、配额 core50/admin50/owner100MB、SVG attachment 防 XSS）
- ✅ 管理后台全套（概览统计、栏目 CRUD、文章管理、用户角色/禁言、申诉裁决、群发通知）—— 统一 `admin/layout.tsx` 鉴权
- ✅ 账户微服务客户端（对齐 account-service 真实路由：accounts/transfers/ledger；Fernet 解密 fishApiKeyEncrypted）+ **投喂 fail-closed**（远端成功才 commit，dev 无服务时本地兜底并告警）
- ✅ 故事阅读页（服务端 marked 渲染）、Turnstile（注册页，env 开关）
- ✅ 游戏：菜单 + **3 局整局移植**（超级井字棋、2048、五子棋含深度搜索 AI）

| 尚未做 / 存量 | 量级 |
|---|---|
| ⬜ 5 个复杂/大型游戏（cube、cubetictactoe 的 3D 渲染；galaxies；speed；connect4 障碍变体；atamas 圆盘引擎）—— 现为诚实的“待移植 + 回源旧版链接”占位，各是独立引擎，需逐个重写 | 大 |
| ⬜ cattca 互动小说渲染器（`.cattca` 现按预格式呈现，留 TODO）| 中 |
| ⬜ 账户微服务**联调**（客户端结构完成，需对真实服务 + 线上 Fernet 密文端到端验证）| 中 |
| ⬜ 申诉“通过后自动撤销原操作”对 delete_blog/comment 的反向逻辑（ban 已支持）| 小 |
| ⬜ 完整样式组件化（现 globals.css + 内联覆盖所需）| 中 |

## 1. 关键架构决策（我替你做的判断）

1. **框架**：Next.js 15（App Router）+ React 19 + TypeScript。理由：SSR/SEO 对博客站是刚需，
   App Router 的 Server Components 能把“Jinja 服务端渲染”几乎 1:1 平移。

2. **ORM**：Prisma，`provider = sqlite`，**直连现有同一个 SQLite 库**。
   → **零数据迁移**：不导数据、不换库，Next 读的就是 Flask 写的那张 `db.db`。
   这是最安全的切换路径，也是你特别叮嘱“注意数据库调用方式”的核心答复。

3. **数据库 schema 归属不变**：仍由 **Flask-Migrate/Alembic** 管理（`alembic_version` 表保留）。
   **绝不**对该库跑 `prisma migrate` —— Prisma 仅作读写客户端。见 [03](03-数据库映射与陷阱.md)。

4. **认证**：密码哈希与 werkzeug **完全兼容**（scrypt / pbkdf2 都支持），用户**无需重置密码**；
   会话换成 JWT（itsdangerous 的 Flask session 无法与 JWT 互通，代价是切换时**需重新登录一次**）。
   见 [04](04-认证与会话迁移.md)。

5. **过渡期共存**：`next.config.mjs` 用 `rewrites` 把 `/auth/avatar/*`、`/image/*` 回源 Flask，
   头像与图床不必第一天就迁。允许 Flask 与 Next 并行灰度。

6. **限频**：Flask 里点赞/评论/投票/图床各写了一份内存限频，这里**抽成一个共享工具**
   `src/lib/rate-limit.ts`（正是 CLAUDE.md 的建议）。多实例部署时应换 Redis。

## 2. 目录

- [01-架构对照](01-架构对照.md) —— Flask 概念 ↔ Next 概念一一对应
- [02-快速开始](02-快速开始.md) —— 如何在本地把 `web-next/` 跑起来
- [03-数据库映射与陷阱](03-数据库映射与陷阱.md) —— **数据库迁移文档（核心）**
- [04-认证与会话迁移](04-认证与会话迁移.md)
- [05-API对照表](05-API对照表.md)
- [06-剩余工作与里程碑](06-剩余工作与里程碑.md)

## 3. 一句话现状

`web-next/` 是一个能 `npm run build` 通过、能 `npm start` 起服务、能对真实数据登录/读写的
**真实可运行地基**，不是 PPT。后续把剩余蓝图按 `blog` 的分层（service → route → page）
复刻即可。
