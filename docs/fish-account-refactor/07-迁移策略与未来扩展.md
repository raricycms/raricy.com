# 07 — 迁移策略与未来扩展

## 1. 三阶段迁移计划

```
Phase 1 (1-2 周)          Phase 2 (1 周)            Phase 3 (1 周)
服务搭建 + 双写             流量切换                   清理
┌─────────────┐          ┌─────────────┐          ┌─────────────┐
│ 搭建 FastAPI │          │ 读操作切远程 │          │ 删旧表旧代码│
│ 实现基础 API │ ──────▶  │ 观察对账      │ ──────▶  │ 模板重构    │
│ 博客侧双写   │          │ 灰度切换      │          │ 完全下线旧  │
│ 对账验证     │          │              │          │ 系统        │
└─────────────┘          └─────────────┘          └─────────────┘
```

### Phase 1：服务搭建 + 双写（预计 1-2 周）

**目标**：账户服务跑起来，博客同时写两边，验证一致性。

**具体步骤：**

1. **搭建账户服务骨架**
   - 创建 `account-service/` 项目
   - 实现：`config.py`, `main.py`, DB session, Alembic
   - 实现 `Account` 和 `LedgerEntry` 模型
   - 初始化系统账户

2. **实现核心 API**
   - `POST /accounts` — 创建账户
   - `GET /accounts/{user_id}/balance` — 查询余额
   - `POST /transfers` — 转账（含幂等）
   - `GET /health` — 健康检查
   - **不实现**流水查询、批量查询（Phase 2 再加）

3. **博客侧新增 `AccountClient`**
   - 实现 `transfer()`, `get_balance()`, `create_account()`
   - 在 `create_app` 中初始化

4. **博客侧双写**
   - 改造签到、投喂、管理员操作：先写本地（不变），再写远程（新增，失败不阻塞）
   - 用户注册时创建远程账户

5. **历史数据迁移**
   ```sql
   -- 1. 为每个有鱼干记录的用户创建 Account
   INSERT INTO account_service.accounts (user_id, currency)
   SELECT id, 'DRIED_FISH' FROM blog.users;

   -- 2. 将历史 FishTransaction 转换为 LedgerEntry
   -- 正数 amount → DEBIT（增加持有）
   -- 负数 amount → CREDIT（减少持有）
   -- 用于初始化期初余额
   ```

6. **运行对账脚本，确认本地和远程一致**

**验收标准：**
- 账户服务 Docker 启动正常
- 签到/投喂双写无报错
- 对账脚本显示 0 差异

---

### Phase 2：流量切换（预计 1 周）

**目标**：读操作切换到账户服务，旧系统只保留写（双写模式持续）。

**具体步骤：**

1. **余额查询切换**
   - `current_user.dried_fish` → `account_client.get_balance(current_user.id)`
   - 排行榜、余额页面改为调远程
   - 注意模板单位转换

2. **流水查询切换**
   - 实现 `GET /ledger` API
   - `fish_transactions` 查询改为调远程
   - `_adapt_tx()` 适配旧模板格式

3. **灰度策略**
   ```python
   # 按用户 ID hash 逐步切换
   import hashlib

   def use_remote_fish(user_id: str) -> bool:
       """根据 hash 决定是否走远程"""
       if not current_app.config.get("FISH_REMOTE_ENABLED", False):
           return False
       hash_val = int(hashlib.md5(user_id.encode()).hexdigest()[:8], 16)
       ratio = current_app.config.get("FISH_REMOTE_RATIO", 0.1)  # 10% → 50% → 100%
       return (hash_val % 100) < (ratio * 100)
   ```

4. **监控观察**
   - 日志中记录每次远程调用的延迟和错误
   - 对账脚本每天跑一次

**验收标准：**
- 100% 流量走远程后无异常
- 连续 3 天对账无差异
- p99 延迟 < 200ms

---

### Phase 3：清理（预计 1 周）

**目标**：删除旧系统所有代码和数据。

**具体步骤：**

1. **数据库清理**
   ```sql
   -- 删除鱼干相关表
   DROP TABLE IF EXISTS fish_transactions;
   DROP TABLE IF EXISTS blog_feeds;  -- 或保留（如果投喂规则仍需本地存储）

   -- 删除 User 上的鱼干字段
   ALTER TABLE users DROP COLUMN IF EXISTS dried_fish;
   ALTER TABLE users DROP COLUMN IF EXISTS total_fortune;
   ```

2. **代码清理**
   - 删除 `app/models/fish.py`
   - 删除 `app/service/fish.py`
   - 删除 `app/models/blog_feed.py`（如果投喂规则改为在账户服务中管理）
   - 删除所有 `FISH_UNIT` 引用
   - 删除所有 `from app.service.fish import ...`

3. **模板重构**
   - 余额展示直接使用 `1.5` 小鱼干而不是 `15000` 内部单位
   - 移除所有 `/ FISH_UNIT` 和 `* 10000` 转换

4. **生成数据库迁移文件**
   ```bash
   flask db migrate -m "移除小鱼干旧系统"
   flask db upgrade
   ```

5. **更新文档**
   - `claude.md`
   - `docs/项目概述与技术架构.md`
   - 新增架构图中标注账户服务

**验收标准：**
- 项目中没有 `dried_fish` / `FISH_UNIT` / `FishTransaction` / `add_fish` / `deduct_fish` 的任何引用
- 所有功能（签到、投喂、排行榜）正常工作
- 测试通过

---

## 2. 未来扩展：模拟炒股

### 2.1 概述

模拟炒股是用户用小鱼干作为交易货币，在模拟市场中进行股票买卖。不涉及真实资金，但交易逻辑要尽量仿真。

### 2.2 新增数据模型

```sql
-- 股票持仓表
CREATE TABLE stock_positions (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    account_id  UUID NOT NULL REFERENCES accounts(id),
    symbol      VARCHAR(20) NOT NULL,     -- 股票代码，如 "600036.SH"
    quantity    BIGINT NOT NULL,          -- 持仓数量（整型，1 股 = 1000 单位）
    avg_cost    BIGINT NOT NULL,          -- 平均成本（内部金额单位）
    created_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at  TIMESTAMPTZ NOT NULL DEFAULT now(),

    CONSTRAINT uq_account_symbol UNIQUE (account_id, symbol)
);

-- 订单表
CREATE TABLE orders (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    account_id  UUID NOT NULL REFERENCES accounts(id),
    symbol      VARCHAR(20) NOT NULL,
    side        VARCHAR(4) NOT NULL CHECK (side IN ('BUY', 'SELL')),
    order_type  VARCHAR(10) NOT NULL DEFAULT 'LIMIT' CHECK (order_type IN ('MARKET', 'LIMIT')),
    price       BIGINT,                   -- 限价（内部金额单位），市价单为 NULL
    quantity    BIGINT NOT NULL,          -- 委托数量
    filled_qty  BIGINT NOT NULL DEFAULT 0, -- 已成交数量
    status      VARCHAR(10) NOT NULL DEFAULT 'OPEN'
                CHECK (status IN ('OPEN', 'PARTIAL', 'FILLED', 'CANCELLED')),
    created_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_orders_account ON orders(account_id, status);
CREATE INDEX idx_orders_symbol ON orders(symbol, status);

-- 成交记录表
CREATE TABLE trades (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    buy_order_id UUID NOT NULL REFERENCES orders(id),
    sell_order_id UUID NOT NULL REFERENCES orders(id),
    symbol      VARCHAR(20) NOT NULL,
    price       BIGINT NOT NULL,          -- 成交价
    quantity    BIGINT NOT NULL,          -- 成交数量
    buyer_account_id UUID NOT NULL REFERENCES accounts(id),
    seller_account_id UUID NOT NULL REFERENCES accounts(id),
    created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

### 2.3 撮合模式

模拟炒股不需要实时撮合。推荐两种模式：

**模式 A：定时批量撮合（推荐）**

```
每 1 分钟（或 5 分钟）：
1. 收集所有 OPEN 订单
2. 从外部 API 获取最新行情价
3. 市价单 → 直接按行情价成交
4. 限价单 → 行情价 <= 买入限价 或 >= 卖出限价 则成交
5. 生成 trades + 更新 positions + 调用 transfer() 划转小鱼干
```

用 Celery Beat 调度：

```python
# celery_app.py
from celery import Celery
from celery.schedules import crontab

app = Celery('account-service')

@app.on_after_configure.connect
def setup_periodic_tasks(sender, **kwargs):
    sender.add_periodic_task(60.0, match_orders.s(), name='match-every-60s')

@app.task
def match_orders():
    """撮合所有待成交订单"""
    # 1. 获取行情
    # 2. 遍历 OPEN 订单
    # 3. 匹配成交
    # 4. 写 trades、更新 positions、更新订单状态
    # 5. 调用 transfer_service.transfer() 划转小鱼干
```

**模式 B：即时撮合（简单版）**

用户下单时立即按当前行情价成交。不需要订单簿，适合初期用户少的时候。

### 2.4 行情源

国内免费行情 API：

| 来源 | 接口 | 限制 |
|------|------|------|
| 新浪财经 | `hq.sinajs.cn` | 免费、稳定、覆盖 A 股 |
| 腾讯财经 | `qt.gtimg.cn` | 免费、实时 |
| EastMoney | `push2.eastmoney.com` | 免费、数据全 |

示例：

```python
import httpx

async def get_stock_price(symbol: str) -> float:
    """获取 A 股实时价格"""
    # 新浪接口格式
    url = f"http://hq.sinajs.cn/list={symbol}"
    async with httpx.AsyncClient() as client:
        resp = await client.get(url)
        # 解析返回文本，提取当前价
        # var hq_str_sh600036="招商银行,42.50,..."
        price = float(resp.text.split('"')[1].split(',')[3])
        return price
```

### 2.5 与小魚干的关系

- **交易货币**：买卖股票用小鱼干结算
- **手续费**：每笔成交扣 0.1% 小鱼干，归系统账户（类似投喂消耗）
- **盈亏**：`(卖出价 - 买入价) × 股数 - 手续费`

```python
# 撮合成交后
# 买方：扣减小鱼干（buy_price × qty + fee）→ 系统账户
# 卖方：系统账户 → 增加小鱼干（sell_price × qty - fee）
# 手续费：买方 + 卖方的手续费 → 系统账户

transfer_service.transfer(
    from_user_id=buyer_id,
    to_user_id="raricy-blog-system",
    amount=buy_price * qty + fee,
    entry_type="stock_buy",
    description=f"买入 {symbol} {qty} 股 @ {buy_price}",
)

transfer_service.transfer(
    from_user_id="raricy-blog-system",
    to_user_id=seller_id,
    amount=sell_price * qty - fee,
    entry_type="stock_sell",
    description=f"卖出 {symbol} {qty} 股 @ {sell_price}",
)
```

### 2.6 未来 API 扩展

```
POST   /api/v1/stocks/orders             # 下单
GET    /api/v1/stocks/orders/{id}         # 查询订单
DELETE /api/v1/stocks/orders/{id}         # 撤单
GET    /api/v1/stocks/orders?status=OPEN  # 订单列表
GET    /api/v1/stocks/positions/{user_id} # 持仓查询
GET    /api/v1/stocks/positions           # 持仓列表（排行榜用）
GET    /api/v1/stocks/trades/{user_id}    # 成交记录
GET    /api/v1/stocks/quotes/{symbol}     # 行情查询（代理外部 API）
```

---

## 3. 更远的可能

### 3.1 多币种支持

`accounts.currency` 字段已预留。未来可以加：

```
DRIED_FISH      — 小鱼干（日常签到/投喂）
STAR_COIN       — 星币（付费阅读专用）
EXPERIENCE      — 经验值（升级用）
```

### 3.2 打赏 / 付费阅读

```json
// 读者打赏作者
POST /api/v1/transfers
{
    "from_user_id": "reader-uuid",
    "to_user_id": "author-uuid",
    "amount": 10.0,
    "entry_type": "tip",
    "description": "打赏文章《xxx》"
}

// 付费阅读
POST /api/v1/transfers
{
    "from_user_id": "reader-uuid",
    "to_user_id": "author-uuid",
    "amount": 5.0,
    "entry_type": "paywall",
    "metadata": {"blog_id": "xxx"}
}
```

### 3.3 交易所式挂单撮合

如果用户量大了，可以从定时批量撮合升级为：

```
订单簿（Order Book）:
  BUY  [price: 42.50, qty: 100] → [price: 42.60, qty: 200]
  SELL [price: 42.70, qty: 150] → [price: 42.80, qty: 50]

用户下单 → 检查对手盘 → 有匹配就立即成交 → 剩余挂单入簿
```

这需要 Redis Sorted Set 做订单簿（按价格排序），撮合引擎用 Lua 脚本在 Redis 中原子执行。但这是很远的事情了。

---

## 4. 时间线总览

```
Week 1-2:  Phase 1 — 搭建账户服务 + 双写
Week 3:    Phase 2 — 流量切换 + 观察
Week 4:    Phase 3 — 清理旧代码
Week 5+:   模拟炒股（独立项目，视情况启动）
```
